import { HilightV2Directive } from './hilight-v2.directive';

describe('HilightV2Directive', () => {
  it('should create an instance', () => {
    const directive = new HilightV2Directive();
    expect(directive).toBeTruthy();
  });
});
