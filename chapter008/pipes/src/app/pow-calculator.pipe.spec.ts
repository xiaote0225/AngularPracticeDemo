import { PowCalculatorPipe } from './pow-calculator.pipe';

describe('PowCalculatorPipe', () => {
  it('create an instance', () => {
    const pipe = new PowCalculatorPipe();
    expect(pipe).toBeTruthy();
  });
});
